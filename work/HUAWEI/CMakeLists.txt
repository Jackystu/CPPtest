file(GLOB SINGLE_FILES "*.cpp" "*.h")
foreach (file ${SINGLE_FILES})
    get_filename_component(target ${file} NAME_WLE)
    add_executable(${target} ${file})
#    target_link_libraries(${target} PRIVATE elog)
endforeach ()